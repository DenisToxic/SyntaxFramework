-- syntax_world/shared/config.lua
-- Default world settings for Syntax

SyntaxWorldConfig = {

    -- Global population toggle (peds & vehicles)
    populationEnabled = false,

    -- Density multipliers (0.0 = none, 1.0 = normal GTA)
    pedDensity        = 0.0,
    vehicleDensity    = 0.0,
    parkedDensity     = 0.0,
    scenarioDensity   = 0.0,

    -- Ambient services / events
    randomCops        = false,
    randomTrains      = false,
    randomBoats       = false,
    idleCops          = false,
}
